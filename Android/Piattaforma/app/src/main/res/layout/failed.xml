<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.fonta.piattaforma.Failed">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="@string/failed"
        android:textSize="20sp"
        android:textColor="@color/colorFailed"
        android:layout_centerHorizontal="true"
        android:id="@+id/failedTextView"
        />

    <ImageView
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/errore"
        android:layout_below="@id/failedTextView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        android:id="@+id/failedImageView"
        />

    <Button
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/failedImageView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="100dp"
        android:background="@color/colorFailed"
        android:text="@string/backToHome"
        android:textColor="@color/colorBottone"
        android:textStyle="bold"
        android:onClick="BackMain"
        />

</RelativeLayout>