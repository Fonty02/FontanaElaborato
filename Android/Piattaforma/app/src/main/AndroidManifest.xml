<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fonta.piattaforma">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcherprimula"
        android:label="PlatformDB"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:label="Piattaforma Covid"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Failed"
            android:label="LoginFallito"

            />

        <activity android:name=".Homepage"
            android:label="Homepage"

            />

        <activity android:name=".Vaccini"
            android:label="Vaccini"

            />

        <activity android:name=".Campagna"
            android:label="Campagna"

            />

        <activity android:name=".FakeNews"
            android:label="FakeNews"

            />

        <activity android:name=".Certificato"
            android:label="Certificato"

            />

        <activity android:name=".nonCertificato"
            android:label="Certificato"

            />

        <activity android:name=".Questionario"
            android:label="Questionario"

            />
    </application>

</manifest>